import task.*;
import manager.*;
import history_manager.*;
import task_manager.*;
import java.util.*;

import java.io.File;

public class Main {

    public static void main(String[] args) {
        System.out.println("Поехали!");// Я отсюда все прошлые спринты убрал и всё, в данном спринте,
        // как и в прошлом здесь вообще ничего не надо, но ладно, верну, просто не знаю зачем оно здесь
        /*TaskManager inMemoryTaskManager = Managers.getDefault();

        inMemoryTaskManager.addTask(new Task("Таск 1", TaskType.TASK, TaskStatus.NEW, "Задача1"));
        inMemoryTaskManager.addTask(new Task("Таск 2", TaskType.TASK, TaskStatus.NEW, "Задача2"));
        inMemoryTaskManager.addEpic(new Epic("Epic 1", TaskType.EPIC, TaskStatus.NEW, "Эпик 1"));
        inMemoryTaskManager.addEpic(new Epic("Epic 2", TaskType.EPIC, TaskStatus.NEW, "Эпик 2"));
        inMemoryTaskManager.addSubTask(new SubTask("SubTask1", TaskType.SUBTASK, TaskStatus.NEW, "Субтаск 1", 3));
        inMemoryTaskManager.addSubTask(new SubTask("SubTask 2", TaskType.SUBTASK, TaskStatus.NEW, "Субтаск 2", 3));
        inMemoryTaskManager.addSubTask(new SubTask("SubTask 3", TaskType.SUBTASK, TaskStatus.NEW, "Субтаск 3", 3));

        for (int i = 1; i <= 2; i++) {
            inMemoryTaskManager.getTask(i);
        }
        for (int i = 3; i <= 4; i++) {
            inMemoryTaskManager.getEpic(i);
        }

        for (int i = 5; i <= 7; i++) {
            inMemoryTaskManager.getSubTask(i);
        }

        for (int i = 1; i <= 2; i++) {
            inMemoryTaskManager.getTask(i);
        }

        System.out.println(inMemoryTaskManager.getHistory());



        inMemoryTaskManager.removeTask(2);

        System.out.println(inMemoryTaskManager.getHistory());

        inMemoryTaskManager.removeEpic(3);

        System.out.println(inMemoryTaskManager.getHistory());*/
    }
}
/*Привет!*/